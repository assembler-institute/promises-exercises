/**
 * Exercise 01:
 *
 * Finish the code of the `exercise01` function
 * so that it:
 *
 * 1. Receives a parameter named `value`
 *
 * 2. returns a Promise.resolve that receives as an argument
 *    the `value` parameter that the `exercise01` function receives
 */

// Finish the code of the function
function exercise01() {}

// Don’t change the code bellow this line
import setup01Exercise from "./01-exercise-setup";

if (process.env.NODE_ENV !== "test") {
    setup01Exercise(exercise01);
}

export default exercise01;
